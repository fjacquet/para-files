---
phase: 05-content-extraction
plan: "03"
type: tdd
wave: 3
depends_on:
  - "05-02"
files_modified:
  - tests/test_content_extraction.py
autonomous: true
requirements:
  - XTRCT-01
  - XTRCT-02
  - XTRCT-03
  - XTRCT-04

must_haves:
  truths:
    - "A test proves an Excel (.xlsx) file with budget content yields sheet names and cell values in the extracted text"
    - "A test proves an ODS file with project plan content yields readable plain text for semantic classification"
    - "A test proves a ZIP file containing invoice_2024.pdf and contract.docx yields a manifest listing both filenames"
    - "A test proves a corrupt Excel file returns a filename fallback string without raising an exception"
    - "A test proves a corrupt ZIP file returns a filename fallback string without raising an exception"
    - "All tests use tmp_path fixture and real file creation — no mocking of file formats"
  artifacts:
    - path: "tests/test_content_extraction.py"
      provides: "Full test suite for Excel, ODS, ZIP, and 7Z content extraction including graceful failure cases"
      min_lines: 80
  key_links:
    - from: "tests/test_content_extraction.py"
      to: "src/para_files/utils/file_utils.py"
      via: "import read_content_preview, _read_excel_file, _read_ods_file, _read_archive_manifest"
      pattern: "from para_files\\.utils\\.file_utils import"
---

<objective>
Write a comprehensive test suite for the content extraction functions added in plans 05-01 and 05-02, using TDD red-green-refactor discipline.

Purpose: Content extractors have defined input/output contracts (file → text) with testable edge cases (corrupt file → filename fallback). TDD ensures the contracts are verified and regression-safe.
Output: tests/test_content_extraction.py with full coverage of Excel, ODS, ZIP, and 7Z readers including graceful failure cases.
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/para_files/utils/file_utils.py
@tests/test_file_utils.py
@.planning/phases/05-content-extraction/05-01-SUMMARY.md
@.planning/phases/05-content-extraction/05-02-SUMMARY.md
</context>

<feature>
  <name>Content extraction tests (Excel, ODS, ZIP/7Z graceful failure)</name>
  <files>tests/test_content_extraction.py, src/para_files/utils/file_utils.py</files>
  <behavior>
Test cases to cover — all using tmp_path fixture with real file creation:

**Excel (.xlsx) tests:**
- `test_excel_xlsx_extracts_sheet_names`: Create xlsx with sheet "Budget_2024", assert "Budget_2024" in result
- `test_excel_xlsx_extracts_cell_values`: Create xlsx with cell value "Total Revenue", assert in result
- `test_excel_dispatch_via_read_content_preview`: Call read_content_preview() on .xlsx — result contains sheet/cell content
- `test_excel_corrupt_returns_filename_fallback`: Write random bytes as .xlsx, assert result starts with "Filename:"
- `test_excel_max_chars_respected`: Create xlsx with many cells, assert len(result) <= max_chars

**ODS tests:**
- `test_ods_extracts_sheet_names`: Create ODS with sheet "Project Plan", assert "Project Plan" in result
- `test_ods_extracts_cell_values`: Create ODS with cell "Milestone 1", assert in result
- `test_ods_corrupt_returns_filename_fallback`: Write random bytes as .ods, assert result starts with "Filename:"
- `test_ods_dispatch_via_read_content_preview`: Call read_content_preview() on .ods — result contains content

**ZIP manifest tests:**
- `test_zip_manifest_lists_filenames`: Create ZIP with invoice_2024.pdf and contract.docx, assert both names in result
- `test_zip_manifest_format`: Assert result starts with "Archive manifest"
- `test_zip_dispatch_via_read_content_preview`: Call read_content_preview() on .zip — result is manifest text
- `test_zip_corrupt_returns_filename_fallback`: Write random bytes as .zip, assert result starts with "Filename:"
- `test_zip_max_chars_respected`: Create ZIP with 500 filenames, assert len(result) <= max_chars

**Integration — read_content_preview dispatch:**
- `test_xlsx_extension_dispatched`: Extension .xlsx routes to Excel reader (not "Filename: X")
- `test_ods_extension_dispatched`: Extension .ods routes to ODS reader (not "Filename: X")
- `test_zip_extension_dispatched`: Extension .zip routes to archive reader (not "Filename: X")

Use `openpyxl` to create real .xlsx test files:
```python
import openpyxl
wb = openpyxl.Workbook()
ws = wb.active
ws.title = "Budget_2024"
ws["A1"] = "Total Revenue"
wb.save(str(tmp_path / "budget.xlsx"))
```

Use `odfpy` to create real .ods test files:
```python
from odf.opendocument import OpenDocumentSpreadsheet
from odf.table import Table, TableRow, TableCell
from odf.text import P
doc = OpenDocumentSpreadsheet()
table = Table(name="Project Plan")
row = TableRow()
cell = TableCell()
cell.addElement(P(text="Milestone 1"))
row.addElement(cell)
table.addElement(row)
doc.spreadsheet.addElement(table)
doc.save(str(tmp_path / "plan.ods"))
```

Use stdlib `zipfile` to create real .zip test files.

Cases that should return "Filename: X":
- `(tmp_path / "bad.xlsx").write_bytes(b"not an xlsx file")` → _read_excel_file returns "Filename: bad.xlsx"
- `(tmp_path / "bad.ods").write_bytes(b"random bytes")` → _read_ods_file returns "Filename: bad.ods"
- `(tmp_path / "bad.zip").write_bytes(b"PK\x00garbage")` → _read_archive_manifest returns "Filename: bad.zip"
  </behavior>
  <implementation>
Follow RED-GREEN-REFACTOR cycle:

RED: Write all test stubs with correct assertions → run `uv run pytest tests/test_content_extraction.py -v` → must fail (functions don't exist yet or tests reveal bugs).

GREEN: Tests should pass without code changes (the implementations from 05-01 and 05-02 already exist). If any test fails due to implementation bugs, fix the implementation in file_utils.py — do NOT weaken test assertions.

REFACTOR: If any test is fragile (e.g., tests internal representation instead of observable behavior), refactor it to test the contract, not the implementation.

Commit sequence:
1. `test(05-03): add failing content extraction tests` (after RED — tests written but before GREEN confirmed)
2. `feat(05-03): content extraction tests passing` (after GREEN confirmed)
  </implementation>
</feature>

<verification>
1. `uv run pytest tests/test_content_extraction.py -v` — all tests pass
2. `uv run pytest tests/test_content_extraction.py --tb=short` — zero failures
3. `uv run ruff check tests/test_content_extraction.py` — no lint violations
4. `uv run mypy tests/test_content_extraction.py` — no type errors
5. `uv run pytest --cov=para_files.utils.file_utils --cov-report=term-missing` — _read_excel_file, _read_ods_file, _read_archive_manifest show meaningful line coverage
</verification>

<success_criteria>
- tests/test_content_extraction.py exists with at minimum 13 test functions
- All tests pass with `uv run pytest tests/test_content_extraction.py`
- Tests use real file creation (openpyxl, odfpy, zipfile) — no mocks of file format parsers
- Graceful failure tests confirm corrupt files return "Filename: X" string, not an exception
- No existing tests broken: `uv run pytest` full suite still passes
</success_criteria>

<output>
After completion, create `.planning/phases/05-content-extraction/05-03-SUMMARY.md`
</output>
