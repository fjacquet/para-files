---
phase: 06-extension-routing
plan: 02
type: tdd
wave: 2
depends_on:
  - "06-01"
files_modified:
  - tests/test_extension_router.py
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
  - ROUTE-04
  - ROUTE-05
  - ROUTE-06

must_haves:
  truths:
    - "All 6 success criteria from the phase are covered by passing tests"
    - "Test for .mp4 → media_video_folder passes"
    - "Test for .mp3 → media_audio_folder passes"
    - "Test for .tif → media_images_folder passes"
    - "Test for .kdbx → security_folder passes"
    - "Test for .sh → scripts_folder passes"
    - "Test for .xyz (unknown) → catchall_folder passes"
    - "Test for file with no extension → returns None"
    - "Test that all EXTENSION_GROUPS keys are covered (parametrize)"
  artifacts:
    - path: "tests/test_extension_router.py"
      provides: "Complete TDD test suite for ExtensionRouterClassifier"
      min_lines: 80
  key_links:
    - from: "tests/test_extension_router.py"
      to: "src/para_files/classifiers/extension_router.py"
      via: "import ExtensionRouterClassifier, EXTENSION_GROUPS"
      pattern: "from para_files.classifiers.extension_router import"
---

<objective>
TDD test suite for ExtensionRouterClassifier covering all 6 ROUTE-* requirements.

Purpose: Automated tests verify that every extension group routes correctly and that the catch-all
and no-extension edge cases behave as specified.

Output: tests/test_extension_router.py — passing test suite (green phase of TDD cycle)
</objective>

<execution_context>
@/Users/fjacquet/.claude/get-shit-done/workflows/execute-plan.md
@/Users/fjacquet/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/06-extension-routing/06-01-SUMMARY.md
@src/para_files/classifiers/extension_router.py
@src/para_files/config.py
@src/para_files/types.py
@tests/test_content_extraction.py
</context>

<feature>
  <name>ExtensionRouterClassifier — full behaviour</name>
  <files>tests/test_extension_router.py, src/para_files/classifiers/extension_router.py</files>
  <behavior>
TDD cycle: RED first (write tests, confirm fail), then GREEN (code already written in plan 01),
then REFACTOR only if needed.

Since ExtensionRouterClassifier is written in plan 01, the RED phase means running the tests
immediately after writing them — they must already pass (GREEN). If any fail, diagnose and fix
the implementation before completing this plan.

Test cases required:

1. Parametrized video test:
   Input: FileMetadata with extension=".mp4" (also .3gp, .m4v, .mov)
   Expected: category == config.media_video_folder, confidence >= 0.95, matched source == EXTENSION_ROUTER

2. Parametrized audio test:
   Input: FileMetadata with extension=".mp3" (also .m4a)
   Expected: category == config.media_audio_folder

3. Parametrized images test:
   Input: FileMetadata with extension=".tif" (also .tiff, .gif, .psd)
   Expected: category == config.media_images_folder

4. Parametrized security test:
   Input: FileMetadata with extension=".kdbx" (also .p7b, .asc)
   Expected: category == config.security_folder, confidence >= 0.97

5. Parametrized scripts test:
   Input: FileMetadata with extension=".sh" (also .ps1, .css, .js)
   Expected: category == config.scripts_folder

6. Unknown extension catch-all test:
   Input: FileMetadata with extension=".xyz"
   Expected: category == config.catchall_folder, confidence == 0.80

7. No extension test:
   Input: FileMetadata with extension=""
   Expected: returns None (no classification)

8. No metadata test:
   Input: metadata=None
   Expected: returns None

9. Full EXTENSION_GROUPS coverage:
   Parametrize over all keys in EXTENSION_GROUPS and assert each returns a non-None result
   with a known category (not catchall_folder).

10. Config override test:
    Create ExtensionRoutingConfig(media_video_folder="custom/video") and verify .mp4
    routes to "custom/video".

Use pytest.mark.parametrize throughout. Create FileMetadata test fixtures with minimal fields
(path=Path("/tmp/test.mp4"), filename="test.mp4", extension=".mp4", size_bytes=1024).
Use `datetime.now()` or None for timestamps.

Follow pattern from tests/test_content_extraction.py for test style.
  </behavior>
  <implementation>
Import ExtensionRouterClassifier and EXTENSION_GROUPS from para_files.classifiers.extension_router.
Import ExtensionRoutingConfig from para_files.config.
Import FileMetadata from para_files.types.
Build a default config fixture using ExtensionRoutingConfig().
Each test constructs FileMetadata directly (no mocking needed — pure logic test).
Assert result.category, result.confidence.value, result.confidence.source.
  </implementation>
</feature>

<verification>
```bash
# RED phase: write tests first, confirm they run (should already pass since implementation exists)
uv run pytest tests/test_extension_router.py -v

# All tests pass

uv run pytest tests/test_extension_router.py -v --tb=short

# Ruff clean

uv run ruff check tests/test_extension_router.py

```
</verification>

<success_criteria>
All tests in tests/test_extension_router.py pass (uv run pytest tests/test_extension_router.py returns 0).
Each of the 6 ROUTE-* requirements maps to at least one passing parametrized test.
No ruff errors in the test file.
</success_criteria>

<output>
After completion, create `.planning/phases/06-extension-routing/06-02-SUMMARY.md`
</output>
